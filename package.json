{
  "private": "true",
  "name": "visual-stack-monorepo",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "precommit": "CI=true lerna run test && lerna run lint"
  },
  "devDependencies": {
    "husky": "^0.12.0",
    "lerna": "2.0.0-beta.32"
  }
}
