{
  "private": "true",
  "name": "visual-stack-monorepo",
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna run build",
    "test": "lerna run test",
    "precommit": "CI=true lerna run test && lerna run lint"
  },
  "devDependencies": {
    "husky": "^0.12.0",
    "lerna": "2.0.0-beta.32"
  },
  "dependencies": {
    "7": "0.0.1",
    "@cjdev/visual-stack": "^2.5.2",
    "bootstrap-loader": "^3.0.0",
    "bootstrap-sass": "^3.3.7"
  }
}
